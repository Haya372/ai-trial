pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "apps/web/**/*.{ts,tsx,js,jsx,json,css}"
      run: ./apps/web/node_modules/.bin/biome format --write {staged_files}
      stage_fixed: true
