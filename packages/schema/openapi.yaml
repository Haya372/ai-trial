openapi: "3.1.0"
info:
  title: AI Trial API
  version: "0.1.0"
paths:
  /health:
    $ref: "./paths/health.yaml"
components:
  schemas:
    HealthResponse:
      $ref: "./components/schemas/HealthResponse.yaml"
