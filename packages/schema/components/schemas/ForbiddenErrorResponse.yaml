type: object
required:
  - code
  - message
properties:
  code:
    type: string
    enum: ["FORBIDDEN"]
    example: "FORBIDDEN"
  message:
    type: string
    example: "You do not have access to this resource"
