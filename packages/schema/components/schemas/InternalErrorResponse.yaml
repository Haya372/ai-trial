type: object
required:
  - code
  - message
properties:
  code:
    type: string
    enum: ["INTERNAL_ERROR"]
    example: "INTERNAL_ERROR"
  message:
    type: string
    example: "Internal server error"
