{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {},
    "lint": {},
    "format": {},
    "bundle": {
      "outputs": ["dist/**"]
    },
    "generate:backend": {
      "dependsOn": ["@ai-trial/schema#bundle"]
    }
  }
}
